# 发行数据的来源与边界

`payload/patch.json.gz` 为声明式改动：对象身份、原始内容哈希、字段路径、中文值、样式数值、新字形指标以及本地图集复制坐标。不是完整资源文件，也不是对压缩游戏资源直接制作的通用二进制差分。

`payload/glyphs.zip` 仅保存从独立生成的 Noto / 朱雀仿宋字体素材中逐字形取出的压缩像素块。每块按 SHA-256 命名。导出时要求像素与许可字体来源完全一致；原游戏字体像素只记录本地复制位置，不放入发行数据。

本地 0.2.0 版覆盖 19 个安装文件中的 2,903 个改变对象；图集重建使用 19,112 次原版字形区域本地复制、81,373 次许可字形块引用，以及原版模式两张 RGBA 图集上的 4,676 次许可 alpha 字形引用。RGBA 原图从玩家原版文件按二维坐标复制，新增白色字形仅从许可字体生成的 alpha 数据构建。引用次数不是去重字符数。保留未修改对象与资源身份。

在受支持原版副本上，完整重建的 19 个输出文件已逐一与当前累计汉化候选进行 SHA-256 比较，全部一致。Bundle 的 Unity 原生 CRC 绑定该最终 SHA；程序只接受这些精确输出，再更新本地 catalog 的 CRC、大小和 hash。没有用安装实测来猜 CRC。

完整的原文库、原始资源、原版备份、生成用 Unity donor bundle、内部游戏测试副本不在公共仓库内。程序没有修改存档的路径，也不包含游戏运行时注入 DLL。

这些技术措施减少官方资源再分发，不代表对游戏译文或原作权利的法律授权。主程序 MIT 与字体 OFL 各有适用范围。
